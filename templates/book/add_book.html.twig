{% extends 'book/index.html.twig' %}

{% block main %}
	<h2>Ajouter un livre à la Bibliothèque</h2>

	{# {{form(form)}} #}

	{{ form_start(form) }}

		{{ form_errors(form) }}
		{# les inputs #}
		{{ form_row(form.title) }}
		{{ form_row(form.date_of_publication) }}

		{{ form_row(form.submit) }}
	{{ form_end(form) }}

	<a href="{{url('list_books')}}">Revenir à la liste des livres</a>
{% endblock %}
